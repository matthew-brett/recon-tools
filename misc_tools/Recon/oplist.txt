#read_image image-path [brs distinguishes brs from epidw]
#for epidw: read_image epidw.fid
#for brs: read_image brs_data.fid brs
read_image standard_brs_data.fid brs

#bal_phs_corr (no params)
bal_phs_corr

#unbal_phs_corr num_ref_rows [default: same as n_pe]
#unbal_phs_corr

#get_fieldmap asems-path threshold-factor [default: 0.1]
#get_fieldmap asems.fid 0.15

#geo_undistort regularization-param
#geo_undistort 8.0

#ifft2d (no params)
ifft2d

#write_image file-name data-type [mag, complex, real, imag, angle, mask, fmap] iteration-dimension [2,3,4,5]
write_image brs.bal mag 4
